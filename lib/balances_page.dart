import 'package:flutter/material.dart';

class BalancesPage extends StatelessWidget {
  const BalancesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Balances Page'),
      ),
      body: Container(),
    );
  }
}
